
' MS VBS Parameters

' Quote support
q = """"

http_proto = "%s"
method = "%s"
path = "%s"

' Content SAVE AS FILES, here shows path
content_path = "%s"

set attr = createobject("Scripting.Dictionary")

%s

'''''''''''''''''''''
